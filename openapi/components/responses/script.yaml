GetScriptResponse:
  description: The found script and its state.
  content:
    application/json:
      schema:
        oneOf:
          - $ref: "../schemas/script.yaml#/V1Script"
          - $ref: "../schemas/script.yaml#/V2Script"
